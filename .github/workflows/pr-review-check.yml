name: "PR Review check"

env:
  TRIGGER_LABELS: '["CI", "preview", "skip-tests"]'
  DEPENDABOT_CREATOR: 'dependabot[bot]'
  MILESTONE_NAME: 'Validating'

on:
  pull_request_review:
    types:
      - submitted
  pull_request:
    branches:
      - develop
    types:
      - labeled

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger validate on approval
        if: github.event_name == 'pull_request_review'
        uses: Alfresco/alfresco-build-tools/.github/actions/github-trigger-approved-pr@6aad0af23482c32f8a4d4ddf68c0ada5a85e2a41 # temp version for testing
        with:
          creator: ${{ env.DEPENDABOT_CREATOR }}
          github-token: ${{ secrets.BOT_GITHUB_TOKEN }}
          milestone-on-approval: ${{ env.MILESTONE_NAME }}
          auto-merge-on-approval: true

      - name: Trigger validation on labeling
        if: github.event_name == 'pull_request'
        uses: Alfresco/alfresco-build-tools/.github/actions/github-trigger-labeled-pr@6aad0af23482c32f8a4d4ddf68c0ada5a85e2a41 # temp version for testing
        with:
          labels: ${{ env.TRIGGER_LABELS }}
          milestone: ${{ env.MILESTONE_NAME }}
