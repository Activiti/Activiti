<?xml version="1.0" encoding="UTF-8" ?> 

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
  
<mapper namespace="org.activiti.cycle.persistence">

  <!-- PROPERTY -->
  
  <resultMap id="propertyResultMap" type="org.activiti.cycle.impl.conf.ConfigEntity">
    <id property="userId" column="USER_" jdbcType="VARCHAR" />
    <result property="xmlConfig" column="VALUE_" jdbcType="VARCHAR" />
    <result property="revision" column="REV_" jdbcType="INTEGER" />
  </resultMap>
  
  <select id="selectDbSchemaVersion" resultType="string">
    select VALUE_ from ACT_GE_PROPERTY where NAME_ = 'schema.version'
  </select>

  <select id="selectProperty" parameterType="string" resultMap="propertyResultMap">
    select * from ACT_CY_CONFIG where USER_ = #{userId}
  </select>
  
  <update id="updateProperty" parameterType="org.activiti.engine.impl.repository.PropertyEntity">
    update ACT_GE_PROPERTY
    <set>
      REV_ = #{newRevision, jdbcType=INTEGER},
      VALUE_ = #{value, jdbcType=VARCHAR},
    </set>
    where NAME_ = #{name, jdbcType=VARCHAR}
      and REV_ = #{revision, jdbcType=INTEGER}
  </update>

  <!-- TABLE DATA -->
  
  <!-- The property passing doesn't seem to work with parameterType='string', so we are forced to use a map here -->
  <select id="selectTableCount" parameterType="org.activiti.engine.impl.TablePageQueryImpl" resultType="long" >
    select count(*) from ${tableName}
  </select>
  
  <select id="selectTableData" parameterType="org.activiti.engine.impl.TablePageQueryImpl" resultType="map">
    select * from ${tableName}
    <if test="orderBy != null">
      order by ${orderBy}
    </if>
  </select>
</mapper>
